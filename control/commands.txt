hostname
lspci | grep -i nvidia || true
ls -l /dev/nvidia* || true
which nvidia-smi || true
nvidia-smi || true
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.device_count())" || true
